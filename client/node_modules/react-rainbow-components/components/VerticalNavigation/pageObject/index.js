"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _classCallCheck2 = _interopRequireDefault(require("@babel/runtime/helpers/classCallCheck"));

var _createClass2 = _interopRequireDefault(require("@babel/runtime/helpers/createClass"));

var PageVerticalItem = require('../../VerticalItem/pageObject');

var PageVerticalSectionOverflow = require('../../VerticalSectionOverflow/pageObject');
/**
 * VerticalNavigation page object class.
 * @class
 */


var PageVerticalNavigation =
/*#__PURE__*/
function () {
  /**
   * Create a new VerticalNavigation page object.
   * @constructor
   * @param {string} rootElement - The selector of the VerticalNavigation root element.
   */
  function PageVerticalNavigation(rootElement) {
    (0, _classCallCheck2["default"])(this, PageVerticalNavigation);
    this.rootElement = rootElement;
  }
  /**
   * Returns a new VerticalItem page object of the element in item position.
   * @method
   * @param {number} itemPosition - The base 0 index of the vertical item.
   */


  (0, _createClass2["default"])(PageVerticalNavigation, [{
    key: "getItem",
    value: function getItem(itemPosition) {
      var items = $(this.rootElement).$$('[data-id="vertical-item"]');

      if (items[itemPosition]) {
        return new PageVerticalItem("".concat(this.rootElement, " [data-id=\"vertical-item\"]:nth-child(").concat(itemPosition + 1, ")"));
      }

      return null;
    }
    /**
     * Returns a new VerticalSectionOverflow page object of the element in item position.
     * @method
     * @param {number} itemPosition - The base 0 index of the vertical section overflow.
     */

  }, {
    key: "getSectionOverflow",
    value: function getSectionOverflow(itemPosition) {
      var items = $(this.rootElement).$$('[data-id="vertical-overflow-container"]');

      if (items[itemPosition]) {
        return new PageVerticalSectionOverflow("".concat(this.rootElement, " [data-id=\"vertical-overflow-container\"]:nth-child(").concat(itemPosition + 1, ")"));
      }

      return null;
    }
  }]);
  return PageVerticalNavigation;
}();

module.exports = PageVerticalNavigation;