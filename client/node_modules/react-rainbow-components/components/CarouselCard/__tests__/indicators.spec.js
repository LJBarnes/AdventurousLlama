"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _react = _interopRequireDefault(require("react"));

var _enzyme = require("enzyme");

var _indicators = _interopRequireDefault(require("../indicators"));

var carouselItems = [{
  indicatorID: 'indicator-1',
  containerID: 'container-1',
  ref: {
    click: jest.fn()
  },
  header: 'Header'
}, {
  indicatorID: 'indicator-2',
  containerID: 'container-2',
  ref: {
    click: jest.fn()
  },
  header: 'Header 2'
}];
describe('<Indicators />', function () {
  it('should render the right amount of indicators', function () {
    var component = (0, _enzyme.mount)(_react["default"].createElement(_indicators["default"], {
      carouselChildren: carouselItems
    }));
    expect(component.find('li').length).toBe(2);
  });
  it('should set the role as tablist in ul element', function () {
    var component = (0, _enzyme.mount)(_react["default"].createElement(_indicators["default"], {
      carouselChildren: carouselItems
    }));
    expect(component.find('ul').prop('role')).toBe('tablist');
  });
});