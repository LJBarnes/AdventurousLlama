"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _classCallCheck2 = _interopRequireDefault(require("@babel/runtime/helpers/classCallCheck"));

var _createClass2 = _interopRequireDefault(require("@babel/runtime/helpers/createClass"));

var PageCarouselCardIndicator = require('./indicator');

var PageCarouselImage = require('./../../CarouselImage/pageObject');
/**
 * CarouselCard page object class.
 * @class
 * @tutorial carouselCard
 */


var PageCarouselCard =
/*#__PURE__*/
function () {
  /**
   * Create a new CarouselCard page object.
   * @constructor
   * @param {string} rootElement - The selector of the CarouselCard root element.
   */
  function PageCarouselCard(rootElement) {
    (0, _classCallCheck2["default"])(this, PageCarouselCard);
    this.rootElement = rootElement;
  }
  /**
   * Returns a new Indicator page object of the element in item position.
   * @method
   * @param {number} itemPosition - The base 0 index of the indicator item.
   */


  (0, _createClass2["default"])(PageCarouselCard, [{
    key: "getIndicatorItem",
    value: function getIndicatorItem(itemPosition) {
      var items = $(this.rootElement).$$('li[role="presentation"]');

      if (items[itemPosition]) {
        return new PageCarouselCardIndicator("".concat(this.rootElement, " li[role=\"presentation\"]:nth-child(").concat(itemPosition + 1, ")"));
      }

      return null;
    }
    /**
     * Returns a new CarouselImage page object of the element in item position.
     * @method
     * @param {number} itemPosition - The base 0 index of the CarouselImage item.
     */

  }, {
    key: "getImageItem",
    value: function getImageItem(itemPosition) {
      var items = $(this.rootElement).$$('li[role="tabpanel"]');

      if (items[itemPosition]) {
        return new PageCarouselImage("".concat(this.rootElement, " li[role=\"tabpanel\"]:nth-child(").concat(itemPosition + 1, ")"));
      }

      return null;
    }
  }]);
  return PageCarouselCard;
}();

module.exports = PageCarouselCard;