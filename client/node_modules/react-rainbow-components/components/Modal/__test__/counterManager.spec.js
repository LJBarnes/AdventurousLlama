"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _counterManager = _interopRequireDefault(require("../counterManager"));

describe('CounterManager', function () {
  it('should set counter to 3', function () {
    _counterManager["default"].increment();

    _counterManager["default"].increment();

    _counterManager["default"].increment();

    expect(_counterManager["default"].counter).toBe(3);
    expect(_counterManager["default"].hasModalsOpen()).toBe(true);
  });
  it('should set counter to 1', function () {
    _counterManager["default"].decrement();

    _counterManager["default"].decrement();

    expect(_counterManager["default"].counter).toBe(1);
    expect(_counterManager["default"].hasModalsOpen()).toBe(true);
  });
  it('should set counter to 0', function () {
    _counterManager["default"].decrement();

    expect(_counterManager["default"].counter).toBe(0);
    expect(_counterManager["default"].hasModalsOpen()).toBe(false);
  });
});