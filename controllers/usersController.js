const db = require("../models");

// defining methods
module.exports = {
    findAll: function (req, res) {
        // db.....don't know what the db collection is called or how to access/see that info
    }




}